# Deployment-specific hardware identities. Keep these values under version
# control with the robot deployment: an empty serial intentionally prevents an
# RGB-D launch from binding an arbitrary compatible USB camera.
astra:
  serial_number: "17121112937"
