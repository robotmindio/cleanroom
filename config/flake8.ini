[flake8]
# Enforce correctness errors without imposing a repository-wide quote/style
# rewrite. compileall covers syntax too; F82 catches undefined names in paths
# that unit tests may not execute (notably safety/error handling).
select = E9,F63,F7,F82
show-source = true
statistics = true
