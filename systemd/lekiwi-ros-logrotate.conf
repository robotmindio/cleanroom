# ROS 2 keeps one log per process plus launch logs.  Cyclone DDS transport
# failures can otherwise fill a disk in hours.
@SERVICE_HOME@/.ros/log/*.log @SERVICE_HOME@/.ros/log/20*/*.log {
    hourly
    maxsize 100M
    rotate 12
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
}
